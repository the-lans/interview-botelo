# interview-botelo

Interview simulator platform for Botelo (backend + auth + email verification).

## Quick start
1. Copy `.env.example` to `.env` and fill secrets.
2. Run the app (TBD).

## Status
Scaffold only. LLM integration pending API key.
